# 使用指南

## 📚 快速开始

### 方式一：使用 Streamlit 界面（推荐）

1. 启动应用：
```bash
streamlit run app.py
```

2. 在浏览器中打开显示的 URL（通常是 http://localhost:8501）

3. 在侧边栏输入你的 OpenAI API Key

4. 在主界面输入简单的 Prompt，点击"开始魔法优化"

### 方式二：在 Python 代码中使用

```python
from optimizer import quick_optimize

# 简单调用
result = quick_optimize(
    user_prompt="写个贪吃蛇游戏",
    scene="Python, 给初学者",
    mode="代码生成 (Coding)"
)

print(result.improved_prompt)
```

### 方式三：高级使用

```python
from optimizer import PromptOptimizer

# 创建优化器实例
optimizer = PromptOptimizer(
    api_key="your-api-key",
    model="gpt-4o"
)

# 优化单个 Prompt
result = optimizer.optimize(
    user_prompt="分析市场趋势",
    scene_desc="电商行业",
    optimization_mode="学术分析 (Academic)"
)

# 查看优化结果
print("思考过程:", result.thinking_process)
print("优化后:", result.improved_prompt)
print("使用技术:", result.enhancement_techniques)
print("新增关键词:", result.keywords_added)

# A/B 对比测试
orig_result, opt_result = optimizer.compare_results(
    original_prompt="分析市场趋势",
    optimized_prompt=result.improved_prompt
)
```

## 🎯 优化模式详解

### 1. 通用增强 (General)
- **适用场景**：日常任务、通用问题
- **优化重点**：明确性、结构化、上下文补充
- **推荐框架**：CO-STAR

**示例**：
- 输入："总结这篇文章"
- 优化后：会补充摘要长度、重点关注内容、目标读者等信息

### 2. 代码生成 (Coding)
- **适用场景**：编程任务、算法实现
- **优化重点**：代码规范、注释、错误处理、示例
- **推荐框架**：RASCEF
- **自动添加**：
  - 代码需包含详细注释
  - 遵循最佳实践
  - 包含错误处理
  - 提供使用示例

**示例**：
- 输入："写个排序算法"
- 优化后：会指定语言、时间复杂度要求、输入输出格式、测试用例等

### 3. 创意写作 (Creative)
- **适用场景**：文案、故事、营销内容
- **优化重点**：创意性、情感共鸣、结构完整
- **推荐框架**：CRISPE

**示例**：
- 输入："写个广告文案"
- 优化后：会明确产品特点、目标受众、情感诉求、行动号召等

### 4. 学术分析 (Academic)
- **适用场景**：研究、论文、数据分析
- **优化重点**：逻辑性、证据支撑、客观性
- **推荐框架**：BROKE

**示例**：
- 输入："分析这个现象"
- 优化后：会要求理论框架、数据支撑、批判性思考、结论建议等

## 🛠️ 核心技术说明

### 1. 语义扩展 (Semantic Expansion)
**原理**：利用 LLM 的理解能力，推断用户隐含的需求

**实现方式**：
- 分析用户意图，识别缺失的信息
- 补充上下文背景
- 明确约束条件
- 规范输出格式

**效果**：
```
输入："写个登录功能"

扩展后包含：
- 需要哪些字段（用户名、密码、记住我）
- 验证规则（长度、格式）
- 错误处理（失败提示、重试次数）
- 安全措施（密码加密、防暴力破解）
```

### 2. 关键词增强 (Keywords Enhancement)
**原理**：识别任务领域，加入专业术语

**实现方式**：
- 提取核心任务关键词
- 联想相关的专业术语
- 用精确词汇替换模糊表达

**效果**：
```
输入："优化网站速度"

增强后的关键词：
- CDN 加速
- 懒加载
- 缓存策略
- 代码压缩
- 图片优化
```

### 3. 模板化重写 (Template Application)
**原理**：使用经过验证的 Prompt 框架

**支持的框架**：

#### CO-STAR 框架
```
Context (背景): 任务的背景信息
Objective (目标): 要达成的具体目标
Style (风格): 输出的风格特点
Tone (语气): 交流的语气
Audience (受众): 目标受众
Response (格式): 期望的输出格式
```

#### BROKE 框架
```
Background (背景): 问题背景
Role (角色): AI 扮演的角色
Objectives (目标): 要完成的目标
Key Results (关键成果): 期望的关键成果
Evolve (改进): 迭代改进的方向
```

#### RASCEF 框架
```
Role (角色): AI 的角色定位
Action (行动): 具体要执行的行动
Steps (步骤): 执行的具体步骤
Context (上下文): 相关背景信息
Examples (示例): 参考示例
Format (格式): 输出格式要求
```

## 📊 效果对比示例

### 示例 1：代码生成

**原始 Prompt**：
```
写个计算器
```

**优化后的 Prompt**：
```
你是一位资深的前端开发工程师，擅长使用 HTML、CSS 和 JavaScript 开发交互式应用。

请设计并实现一个功能完整的网页计算器。

要求：
1. 功能需求：
   - 支持基本四则运算（加、减、乘、除）
   - 支持小数点运算
   - 包含清除(C)和退格(DEL)功能
   - 显示计算过程和结果

2. 技术实现：
   - 使用纯 JavaScript 实现，无需框架
   - 代码需包含详细的中文注释
   - 遵循 ES6+ 语法规范
   - 实现防抖处理，避免重复点击

3. 界面设计：
   - 响应式布局，适配手机和电脑
   - 美观的按钮样式和配色
   - 数字显示清晰易读

4. 代码结构：
   - HTML 结构清晰
   - CSS 样式模块化
   - JavaScript 逻辑分离

5. 输出格式：
   - 提供完整的 HTML 文件
   - 代码可直接在浏览器中运行
   - 包含简单的使用说明

目标受众：初级前端开发者，通过此项目学习事件处理和 DOM 操作。
```

**提升效果**：
- ✅ 明确了技术栈（HTML/CSS/JavaScript）
- ✅ 列出了完整的功能需求
- ✅ 规范了代码质量（注释、规范）
- ✅ 考虑了用户体验（响应式、防抖）
- ✅ 指定了输出格式

### 示例 2：文案创作

**原始 Prompt**：
```
写个产品介绍
```

**优化后的 Prompt**：
```
你是一位专业的品牌营销文案策划师，擅长用情感化的语言打动消费者。

背景：某科技公司即将推出一款面向年轻用户的智能手表新品

任务目标：
为这款智能手表撰写一篇产品介绍文案，用于官网展示和社交媒体传播。

风格要求：
- 年轻化、充满活力
- 强调科技感与时尚感的结合
- 语言简洁有力，富有感染力

内容要点：
1. 开头：用一句话抓住注意力，点出产品核心价值
2. 功能亮点：突出 3-5 个最吸引人的功能（健康监测、运动追踪、智能提醒等）
3. 使用场景：描绘用户的真实使用场景，引发共鸣
4. 情感诉求：传达产品带给用户的生活方式改变
5. 行动号召：引导用户产生购买兴趣

目标受众：
18-35 岁的都市年轻人，关注健康、科技和潮流

输出格式：
- 标题（10 字以内）
- 副标题（20 字左右）
- 正文（300-500 字）
- 结尾行动号召（1 句话）

请确保文案既有信息量，又能激发情感共鸣。
```

## 🎓 最佳实践

### 1. 输入 Prompt 的技巧
- ✅ **尽量简洁**：系统会自动扩展，不需要写很详细
- ✅ **描述意图**：说清楚你想做什么
- ✅ **添加场景**：在场景描述中补充关键信息

### 2. 选择合适的优化模式
- 编程相关 → 代码生成
- 写作营销 → 创意写作
- 研究分析 → 学术分析
- 其他任务 → 通用增强

### 3. 利用 A/B 测试
- 运行对比测试，直观看到优化效果
- 根据结果调整输入或模式

### 4. API Key 管理
- 建议在 .env 文件中配置，不要硬编码
- 使用代理服务时配置 base_url

## 🔧 故障排查

### 问题 1：优化失败
**可能原因**：
- API Key 无效或过期
- 网络连接问题
- API 配额用尽

**解决方案**：
- 检查 API Key 是否正确
- 确认网络连接正常
- 查看 OpenAI 账户余额

### 问题 2：优化效果不佳
**可能原因**：
- 输入 Prompt 过于模糊
- 选择的优化模式不合适
- 使用的模型版本较低

**解决方案**：
- 在场景描述中补充更多信息
- 切换到其他优化模式尝试
- 使用 GPT-4o 模型

### 问题 3：JSON 解析错误
**可能原因**：
- 模型返回格式不规范

**解决方案**：
- 系统会自动使用备用优化方案
- 检查是否使用了支持 JSON mode 的模型

## 💡 进阶用法

### 自定义优化策略
编辑 `templates.py`，添加自己的优化策略：

```python
SCENE_STRATEGIES["我的场景"] = {
    "focus": ["clarity", "examples"],
    "template": "CO-STAR",
    "extra_requirements": [
        "自定义要求1",
        "自定义要求2"
    ]
}
```

### 批量处理
```python
from optimizer import PromptOptimizer

optimizer = PromptOptimizer()
prompts = ["任务1", "任务2", "任务3"]

results = optimizer.batch_optimize(
    prompts=prompts,
    scene_desc="统一场景",
    optimization_mode="通用增强 (General)"
)

for prompt, result in zip(prompts, results):
    print(f"{prompt} -> {result.improved_prompt}")
```

### 集成到自己的应用
```python
from optimizer import PromptOptimizer

def my_app(user_input):
    optimizer = PromptOptimizer()
    optimized = optimizer.optimize(user_input)
    
    # 使用优化后的 Prompt 调用你的 LLM
    final_result = your_llm_call(optimized.improved_prompt)
    
    return final_result
```

## 📞 获取帮助

- 查看 [README.md](README.md) 了解项目概况
- 运行 `python examples.py` 查看更多示例
- 遇到问题请提 Issue

---

**祝你使用愉快！🚀**
